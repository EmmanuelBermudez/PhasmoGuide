// auth.js

function registerUserToController(name, email, password) {
    // Aquí puedes hacer una solicitud a tu backend
    // Por simplicidad, usaremos una llamada síncrona simulada

    // Simulación de registro exitoso
    console.log(`Registrando usuario: ${name}, ${email}`);
    return true;
}

function loginUserToController(email, password) {
    // Aquí puedes hacer una solicitud a tu backend
    // Por simplicidad, usaremos una llamada síncrona simulada

    // Simulación de inicio de sesión exitoso
    console.log(`Iniciando sesión con: ${email}`);
    return true;
}
